<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="/new">create a message</a>
    <ul>
      <% for(let i=0; i<messages.length; i++) { %>
          <li>
              <%= messages[i].user %>
              <%= messages[i].text %>
              <%= messages[i].added %>
          </li>
      <% } %>
  </ul>
  
  </body>
</html>
